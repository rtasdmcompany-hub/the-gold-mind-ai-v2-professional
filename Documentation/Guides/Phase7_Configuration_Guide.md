# Configuration Guide

Configuration Center (`ECC`) manages profile/template/workspace catalogs. Template apply is blocked while AI-managed trades are open. Never writes live risk parameters into Core.
