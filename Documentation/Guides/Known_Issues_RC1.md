# Known Issues — RC-1

1. **Empty backtest metrics** until own Magic closed deals exist (Info).  
2. **Volume step rounding** may warn on partial-close runner checks (Minor).  
3. **Memory available** terminal property may report 0 — fail-safe skips low-memory gate (Info).  
4. **AI / Dashboard / Hedge** intentionally not implemented (Deferred).

No critical blockers for RC-1 live candidate use under Production Mode.
